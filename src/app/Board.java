package app;

public class Board {
    Cell[][] cells;


    // set the new state for every one of our cells, regenerate the board to new cell values after given time
    // interval (have a temp board that we set this.cells equal to)


}

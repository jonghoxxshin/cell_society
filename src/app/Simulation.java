package app;

public class Simulation {

    double timeTillChange; // note - represent in seconds?
    double probability; // probability we successfully move from one state to another on an event defined in app.Rules.
                        // In a normal game, this would be 100%, but in another game, user defined
                        // so, we'd want smulation

    // if we're using java fx, e

    // control how fast/slow board changes the state

    // once board has changed the state, we want to take the board and display it on app.Main


    // use the app.Rules object created by app.RulesParser to dictate changes in app.Board
}
